async function mintHotel(name: string, rooms: bigint, shares: bigint) {
  // here lets build a transaction to mint
  // the single hotel NFT
  // the required rooms NFTs
  // the required shares FTs
}
